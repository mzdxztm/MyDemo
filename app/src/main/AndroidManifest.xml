<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ztm.mydemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".testActivity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".testActivity.RefereshTestActivity" />
        <activity android:name=".testActivity.PhotoCheckTestActivity" />
        <activity android:name=".photo.PhotoViewActivity" />
        <activity android:name=".testActivity.LetterIndexActivity" />
        <activity android:name=".testActivity.PartExpandRvListActivity" />
        <activity android:name=".DataBindingTestActivity" />
        <activity android:name=".testActivity.TwowayViewActivity" />
        <activity android:name=".testActivity.ImgProportionTestActivity"></activity>
    </application>

</manifest>